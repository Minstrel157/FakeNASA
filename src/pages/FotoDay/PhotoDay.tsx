import React, { FC } from "react";
import styles from "./PhotoDay.module.scss"
const FotoDay: FC = () =>
{
    return(
        <div className={styles.PhotoDayContainer}>
            
        </div>
    )
}

export default FotoDay